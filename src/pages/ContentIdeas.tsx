
import Layout from "@/components/Layout";
import BackButton from "@/components/BackButton";

const ContentIdeas = () => {
  return (
    <Layout>
      <div className="fade-in">
        <div className="mb-4">
          <BackButton />
        </div>
        {/* Empty Content Ideas page */}
      </div>
    </Layout>
  );
};

export default ContentIdeas;
